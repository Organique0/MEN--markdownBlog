<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootswatch@5.2.3/dist/vapor/bootstrap.min.css"
      integrity="sha384-..."
      crossorigin="anonymous"
    />
    <title>Blog</title>
  </head>
  <body style="height: 100vh">
    <div class="container">
      <h1 class="mb-4 mt-4"><%=article.title%></h1>
      <div class="text-muted mb-2">
        <%= article.createdAt.toLocaleDateString()%>
      </div>
      <a href="/" class="btn btn-secondary mt-3">All articles</a>
      <a href="./edit/<%=article.id%>" class="btn btn-secondary mt-3">Edit</a>
      <div class="border border-3 border-warning px-4 pt-2 my-5">
        <%- article.sanitizedHtml%>
      </div>
    </div>
  </body>
</html>
